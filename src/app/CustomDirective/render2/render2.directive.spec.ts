import { Render2Directive } from './render2.directive';

describe('Render2Directive', () => {
  it('should create an instance', () => {
    const directive = new Render2Directive();
    expect(directive).toBeTruthy();
  });
});
