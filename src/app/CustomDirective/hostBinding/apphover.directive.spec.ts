import { ApphoverDirective } from './apphover.directive';

describe('ApphoverDirective', () => {
  it('should create an instance', () => {
    const directive = new ApphoverDirective();
    expect(directive).toBeTruthy();
  });
});
